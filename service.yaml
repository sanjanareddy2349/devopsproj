apiVersion: v1
kind: Service
metadata:
  name: pinterestdemo-service
spec:
  selector:
    app: pinterestdemo
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30188
  type: NodePort